<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- State saat item dipilih (checked) -->
    <item android:drawable="@drawable/ic_list" android:state_checked="true" android:tint="?attr/colorPrimary" />

    <!-- State default (tidak dipilih) -->
    <item android:drawable="@drawable/ic_list" android:tint="?attr/colorOnSurfaceVariant" />
</selector>